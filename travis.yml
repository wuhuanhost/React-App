language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

# script:
  # - hexo g

after_script:
  - cd ./dist
  - git init
  - git config user.name "wuhuanhost"
  - git config user.email "post.thinking@gmail.com"
  - git add .
  - git commit -m "Update React-App"
  - git push --force --quiet "https://${gh-token}@${GH_REF}" origin:gh-pages
# E: Build LifeCycle

branches:
  only:
    - gh-pages
env:
 global:
   - GH_REF: git@github.com:wuhuanhost/React-App.git